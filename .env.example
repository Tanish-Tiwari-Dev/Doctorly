# Copy to .env and fill in real values, OR pass --dart-define=SUPABASE_URL=... at run time.
# Both are supported; .env takes precedence when present.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
